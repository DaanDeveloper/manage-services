# Service Desk

A lightweight macOS desktop app for managing local developer services such as Redis and Typesense.

## Development

```bash
npm install
npm run tauri dev
```

## Scripts

```bash
npm run dev        # Frontend only
npm run build      # TypeScript + Vite build
npm run tauri dev  # Desktop app in development
```
